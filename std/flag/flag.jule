// Copyright 2023 The Jule Project Contributors. All rights reserved.
// Use of this source code is governed by a BSD 3-Clause
// license that can be found in the LICENSE file.

use "std/comptime"
use "std/conv"
use "std/strings"

const bitSize = 0b1000000

// Common behaviors of flags.
trait Common {
	// Returns name of flag.
	fn Name(*self): string

	// Returns short name of flag.
	fn Short(*self): rune

	// Returns description of flag.
	fn What(*self): string

	// Resets data to default.
	fn Reset(mut *self)
}

// A Flag for FlagSet.
struct Flag[T] {
	name:    string
	short:   rune
	default: T
	what:    string
	data:    &T
}

impl Common for Flag {
	fn Name(*self): string { return self.name }
	fn What(*self): string { return self.what }
	fn Reset(mut *self) { *self.data = self.default }
	fn Short(*self): rune { return self.short }
}

// Flag for i64 type.
type Int = &Flag[i64]

// Flag for u64 type.
type Uint = &Flag[u64]

// Flag for f64 type.
type Float = &Flag[f64]

// Flag for bool type.
type Bool = &Flag[bool]

// Flag for string type
type String = &Flag[string]

// Flag parser for command-line arguments.
//
// Syntax:
//	Long names can be used with double dash (--). Short names can be
//	used with a single dash (-). When Boolean flags are used, they use
//	the opposite of their default values. Floating-point values are the
//	same as the [ParseFloat] function provided by the "std/conv" package.
//	Decimal, octal, binary and hexadecimal formats are supported for
//	signed and unsigned integer types. String types accept values ​​directly.
//
//	Octal values are represented by starts with 0o or 0 prefix.
//	Hexadecimal values are represented by starts with 0x prefix.
//	Binary values are represented by starts with 0b prefix.
//
//	A space is required to give a value. When a single dash (-) is used,
//	all following characters are considered short names and thus collective
//	use is allowed. If the short name flags used need values, the values ​
//	should follow respectively.
struct Set {
	flags: []Common
}

impl Set {
	// Returns new flagset.
	fn New(): &Set {
		return &Set{
			flags: make([]Common, 0, 20),
		}
	}
}

impl Set {
	// Returns flag by name, returns nil if not exist.
	fn FindFlag(mut *self, name: string): Common {
		for (_, mut flag) in self.flags {
			if flag.Name() == name {
				return flag
			}
		}
		return nil
	}

	// Returns flag by short name, returns nil if not exist.
	fn FindFlagShort(mut *self, name: rune): Common {
		for (_, mut flag) in self.flags {
			if flag.Short() == name {
				return flag
			}
		}
		return nil
	}

	// Returns all flags.
	fn Flags(mut *self): []Common {
		return self.flags
	}

	// Parse arguments and process flags.
	// Returns non-flag content.
	// Error is always string and holds error message.
	fn Parse(mut *self, args: []string)!: []string {
		mut ap := argParser{
			set:  *self,
			args: unsafe { *(&args) },
		}
		ap.parse()?
		return ap.content
	}

	// Resets all flags to default value.
	fn Reset(mut *self) {
		for (_, mut f) in self.flags {
			f.Reset()
		}
	}

	fn addFlagCommon[T](mut *self, name: string, short: rune, what: string): &Flag[T] {
		if self.FindFlag(name) != nil {
			panic("flag: FlagSet.Add[T]: flag is already exist in this name: " + name)
		}

		if short != 0 && self.FindFlagShort(short) != nil {
			panic("flag: FlagSet.Add[T]: flag is already exist in this short name: " + string(short))
		}

		mut flag := &Flag[T]{
			name:  name,
			short: short,
			what:  what,
		}
		self.flags = append(self.flags, flag)
		return flag
	}

	// Adds new flag and returns allocated reference variable.
	// Panics if name or short name is alreadys exist.
	// Zero (0) short names will be ignored.
	// Panics if used unsupported type.
	fn Add[T: i64 | u64 | f64 | bool | string](mut *self, name: string, short: rune, default: T, what: string): &T {
		mut flag := self.addFlagCommon[T](name, short, what)
		flag.data = new(T, default)
		return flag.data
	}

	// Same with the Add method but do not allocates new reference, uses existing.
	fn AddVar[T: i64 | u64 | f64 | bool | string](mut *self, mut var: &T, name: string, short: rune, what: string) {
		if var == nil {
			panic("flag: FlatSet.AddVar[T]: variable is nil")
		}
		mut flag := self.addFlagCommon[T](name, short, what)
		flag.default = *var
		flag.data = var
	}
}

struct argParser {
	set:     Set
	args:    []string
	content: []string
	i:       int
}

impl argParser {
	fn parseInteger[T](*self, s: string, parser: fn(s: string, base: int): (T, bool)): (x: T, ok: bool) {
		match {
		| s == "":
			return 0, false
		| strings::HasPrefix(s, "0x"): // Hexadecimal
			x, ok = parser(s[2:], 0b00010000)
		| strings::HasPrefix(s, "0b"): // Binary
			x, ok = parser(s[2:], 0b10)
		| strings::HasPrefix(s, "0o"): // Octal
			x, ok = parser(s[2:], 0b1000)
		| s[0] == '0': // Octal
			x, ok = parser(s[1:], 0b1000)
		|:
			// Decimal
			x, ok = parser(s, 0b1010)
		}
		return
	}

	fn getData(mut *self): string {
		if self.i+1 >= len(self.args) {
			return ""
		}
		self.i++
		return self.args[self.i]
	}

	fn processFlag(mut *self, mut flag: Common)! {
		match type flag {
		| Bool:
			mut bf := flag.(Bool)
			*bf.data = !bf.default
			return
		}

		data := self.getData()
		if data == "" {
			throw "--" + flag.Name() + ": expression missing"
		}

		match type flag {
		| Int:
			mut intf := flag.(Int)
			(*intf.data), ok := self.parseInteger[i64](data, fn|s, base| {
				i := conv::ParseInt(s, base, bitSize) catch { return 0, false }
				return i, true
			})
			if !ok {
				throw "--" + flag.Name() + ": expression must be valid signed integer"
			}
		| Uint:
			mut uintf := flag.(Uint)
			(*uintf.data), ok := self.parseInteger[u64](data, fn|s, base| {
				i := conv::ParseUint(s, base, bitSize) catch { return 0, false }
				return i, true
			})
			if !ok {
				throw "--" + flag.Name() + ": expression must be valid unsigned integer"
			}
		| Float:
			mut ff := flag.(Float)
			*ff.data = conv::ParseFloat(data, bitSize) catch {
				throw "--" + flag.Name() + ": expression must be valid floating-point"
			}
		| String:
			*flag.(String).data = data
		}
	}

	fn singleDash(mut *self, arg: string)! {
		if len(arg) == 1 { // Just dash.
			throw arg + ": missing argument"
		}
		// Start at 1 to skip dash.
		for _, r in arg[1:] {
			mut flag := self.set.FindFlagShort(r)
			if flag == nil {
				throw arg + ": flag is not exist: " + string(r)
			}
			self.processFlag(flag)?
		}
	}

	fn doubleDash(mut *self, arg: string)! {
		if len(arg) == 2 { // Just dash.
			throw arg + ": missing argument"
		}
		mut flag := self.set.FindFlag(arg[2:]) // Start at 2 to skip dashes.
		if flag == nil {
			throw arg + ": flag is not exist"
		}
		self.processFlag(flag)?
	}

	fn tryArg(mut *self)! {
		arg := self.args[self.i]
		if len(self.args) == 0 {
			return
		}
		if arg[0] == '-' {
			if len(arg) > 1 && arg[1] == '-' {
				self.doubleDash(arg)?
				return
			}
			self.singleDash(arg)?
			return
		}
		self.content = append(self.content, arg)
	}

	fn parse(mut *self)! {
		for self.i < len(self.args); self.i++ {
			self.tryArg()?
		}
	}
}