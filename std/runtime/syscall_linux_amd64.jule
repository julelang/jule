// Copyright 2026 The Jule Project Contributors. All rights reserved.
// Use of this source code is governed by a BSD 3-Clause
// license that can be found in the LICENSE file.

use "std/sys"

fn syscall_Mmap(addr: *unsafe, length: uintptr, prot: int, flags: int, fd: int, offset: i64): *unsafe {
	r0, _ := syscall(sys::SYS_MMAP, uintptr(addr), length, uintptr(prot), uintptr(flags), uintptr(fd), uintptr(offset))
	return unsafe { (*unsafe)(r0) }
}