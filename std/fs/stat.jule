// Copyright 2022-2024 The Jule Programming Language.
// Use of this source code is governed by a BSD 3-Clause
// license that can be found in the LICENSE file.

use std::sys::{self, S_IFDIR, S_IFREG}

// Status information.
struct Status {
    // Type and mode.
    Mode: uint

    // Total size in bytes of regular file or symbolic link.
    Size: uint
}

impl Status {
    // Reports path is directory or not.
    fn IsDir(self): bool { ret self.Mode&S_IFDIR == S_IFDIR }

    // Reports path is regular file or not.
    fn IsReg(self): bool { ret self.Mode&S_IFREG == S_IFREG }
}