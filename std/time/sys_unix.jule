// Copyright 2024 The Jule Project Contributors. All rights reserved.
// Use of this source code is governed by a BSD 3-Clause
// license that can be found in the LICENSE file.

use "std/sys"

fn open(name: string): (uintptr, ok: bool) {
	fd := sys::Open(name, sys::O_RDONLY, 0) catch {
		return 0, false
	}
	return uintptr(fd), true
}

fn read(fd: uintptr, mut buf: []byte): (n: int, ok: bool) {
	if len(buf) == 0 {
		// If the caller wanted a zero byte read, return immediately
		// without trying to read.
		return 0, true
	}
	ok = true
	n = sys::Read(int(fd), buf) catch {
		ok = false
		use 0
	}
	return
}

fn preadn(fd: uintptr, mut buf: []byte, off: int): (ok: bool) {
	mut whence := seekStart
	if off < 0 {
		whence = seekEnd
	}
	sys::Seek(int(fd), i64(off), whence) catch {
		return false
	}
	for len(buf) > 0 {
		m := sys::Read(int(fd), buf) catch {
			return false
		}
		buf = buf[m:]
	}
	return true
}

fn closefd(fd: uintptr) {
	sys::Close(int(fd))!
}