// Copyright 2022 The Jule Project Contributors. All rights reserved.
// Use of this source code is governed by a BSD 3-Clause
// license that can be found in the LICENSE file.

use "std/bufio"
use "std/conv"
use "std/fmt"
use "std/os"

let mut scan = bufio::Scanner.New(os::Stdin())

fn readLine(): string {
	scan.Scan()!
	return scan.Text()
}

fn numericInput(msg: string)!: f64 {
	fmt::Print(msg)
	input := readLine()
	return conv::ParseFloat(input, 64) catch { throw false }
}

fn main() {
	for {
		let l = numericInput("Input left: ") catch {
			fmt::Println("Invalid left!")
			continue
		}
		let r = numericInput("Input right: ") catch {
			fmt::Println("Invalid right!")
			continue
		}
		fmt::Print("Choose your op: + - / *\nYour op: ")
		let input = readLine()
		match input {
		| "+":
			fmt::Println(l + r)
		| "-":
			fmt::Println(l - r)
		| "*":
			fmt::Println(l * r)
		| "/":
			fmt::Println(l / r)
		|:
			fmt::Println("Invalid operation!")
		}
	}
}