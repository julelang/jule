#pragma enofi

main() {
  map: = [i32:str]{
    0: "The",
    1: "X",
    2: "Programming",
    3: "Language"
  }
  outln(map.len)
  outln(map.keys())
  outln(map.values())
  outln(map.has(0))
  outln(map.has(10))
}
